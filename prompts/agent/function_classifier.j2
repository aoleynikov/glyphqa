You are an expert at analyzing JavaScript functions and classifying them into logical domains.

## Task
Analyze the provided function and classify it into the most appropriate domain.

## Function Information
- **Name**: {{ function_name }}
- **Type**: {{ function_type or 'unknown' }}
- **Content**: {{ function_content }}

## Application Context
{{ application_context or 'No specific context provided' }}

## Available Domains
- **auth**: Authentication, login, logout, session management
- **navigation**: Page navigation, routing, menu interactions
- **forms**: Form interactions, data entry, form validation
- **validation**: Assertions, checks, waiting, verification
- **data_management**: CRUD operations, data manipulation
- **ui_interaction**: UI element interactions, clicks, hovers
- **utilities**: Helper functions, common utilities, base functions

## Classification Rules
1. Analyze the function's primary purpose
2. Consider the function name, content, and context
3. Choose the most appropriate domain
4. Provide confidence level (0.0 to 1.0)
5. Explain your reasoning

## Output Format
Return a JSON object with the following structure:
```json
{
  "domain": "chosen_domain",
  "confidence": 0.85,
  "reasoning": "Brief explanation of why this domain was chosen",
  "suggested_module": "module_name"
}
```

## Examples
- Function named "loginAsAdmin" → domain: "auth"
- Function named "navigateToDashboard" → domain: "navigation"  
- Function named "fillLoginForm" → domain: "forms"
- Function named "verifyDashboard" → domain: "validation"
- Function named "createUser" → domain: "data_management"
- Function named "clickButton" → domain: "ui_interaction"
- Function named "waitForElement" → domain: "utilities"

Return ONLY the JSON object, no explanations.
