You are an expert at analyzing test scenarios and determining which SDK functions are needed.

## Task
Analyze the provided scenario and determine which SDK functions are required for implementation.

## Scenario Information
- **Name**: {{ scenario_name }}
- **Steps**: 
{% for step in scenario_steps %}
  - {{ step }}
{% endfor %}

## Available SDK Functions
{{ available_functions }}

## Analysis Rules
1. Identify the core actions needed for each step
2. Map actions to appropriate SDK functions
3. Consider function dependencies and prerequisites
4. Prioritize reusable functions over scenario-specific ones
5. Include both action and verification functions

## Output Format
Return a JSON object with the following structure:
```json
{
  "needed_functions": [
    "function1",
    "function2",
    "function3"
  ],
  "reasoning": "Brief explanation of why these functions are needed",
  "dependencies": {
    "function1": ["function2", "function3"],
    "function2": ["function4"]
  }
}
```

## Examples
- Login scenario → ["loginAsAdmin", "verifyDashboard", "waitForLoginPage"]
- User creation → ["createUser", "verifyUserCreated", "fillForm"]
- Navigation → ["navigateToPage", "verifyPageLoaded", "clickNavigationLink"]

Return ONLY the JSON object, no explanations.
