Analyze the following scenarios for circular dependencies. A circular dependency occurs when scenario A depends on scenario B, and scenario B depends on scenario A (directly or indirectly).

IMPORTANT: Only consider dependencies between these actual scenario names: {{ actual_scenario_names | join(', ') }}

Scenarios:
{% for name, content in scenarios.items() %}
=== {{ name }} ===
{{ content }}

{% endfor %}
Please analyze these scenarios and determine:

1. Are there any circular dependencies between the actual scenario names listed above?
2. If yes, list the circular dependency chains using only the actual scenario names

Respond in this exact format:
CIRCULAR_REFERENCES: [true/false]
CHAINS: [list of circular chains, one per line, or "none" if no circular references]

Example:
CIRCULAR_REFERENCES: true
CHAINS: 
scenario_a -> scenario_b -> scenario_c -> scenario_a
scenario_x -> scenario_y -> scenario_x

Or if no circular references:
CIRCULAR_REFERENCES: false
CHAINS: none
